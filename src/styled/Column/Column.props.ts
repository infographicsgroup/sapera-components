import {
  BackgroundProps,
  BorderProps,
  ColorProps,
  FlexboxProps,
  LayoutProps,
  PositionProps,
  SpaceProps,
  GridProps,
} from "styled-system";

type ColumnProps = BackgroundProps &
  BorderProps &
  ColorProps &
  FlexboxProps &
  LayoutProps &
  LayoutProps &
  PositionProps &
  SpaceProps &
  GridProps;

export { ColumnProps };
